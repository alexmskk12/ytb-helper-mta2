{
  "name": "ytbradio-helper",
  "version": "1.0.0",
  "description": "Servidor auxiliar para FFS Radio - resolve URLs de stream do YouTube",
  "main": "server.js",
  "scripts": {
    "build": "pip install yt-dlp --break-system-packages || pip3 install yt-dlp --break-system-packages || true",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
